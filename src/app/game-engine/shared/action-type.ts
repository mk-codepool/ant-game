/**
 * Enum defining types of actions that consume energy
 */
export enum ActionType {
  /**
   * Movement action - consumes energy based on distance traveled
   */
  MOVE = 'MOVE',
  // Future action types can be added here:
  // ATTACK = 'ATTACK',
  // EAT = 'EAT',
  // REPRODUCE = 'REPRODUCE',
}
